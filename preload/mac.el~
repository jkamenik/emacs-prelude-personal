;;; Mac - things need to setup for the mac

(setq is-mac (equal system-type 'darwin))

(when is-mac
      (require-package 'exec-path-from-shell)
      (exec-path-from-shell-initialize))

(provide 'mac)
