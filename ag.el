;;; Ag - provide ag settings

(setq-default ag-highlight-search t)
(setq-default ag-reuse-window t) ;; just open the file, not a new editor
(setq-default ag-reuse-buffers t) ;; just have one search screen

(provide 'personal-ag)
